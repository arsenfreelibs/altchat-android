# Настройка Shadowsocks-2022 для пула авто-прокси Alt Chat

Пошагово: поднять `shadowsocks-rust` на VPS, собрать корректные SIP002 `ss://`-ссылки
и положить их в `proxy.properties`. Решает «не работает без VPN» в РФ
(см. диагноз в `PROXY_DPI_ISSUE.md`).

Делать на **каждом** из 3–5 VPS (разные хостинги / разные IP-подсети — для
отказоустойчивости при IP-блокировках).

---

## 0. Требования к VPS

- Любой дешёвый VPS, ~$3–5/мес (Hetzner, Vultr, мелкие провайдеры — что угодно вне РФ).
- Debian 12 / Ubuntu 22.04+.
- Открыть в фаерволе **один TCP-порт** под Shadowsocks (ниже — `8388`, лучше
  выбрать нестандартный высокий порт, напр. `21043`).
- Желательно у каждого сервера свой IP в разной подсети.

---

## 1. Установка shadowsocks-rust

Берём официальные бинарники (не `shadowsocks-libev` — нам нужны 2022-шифры).

```bash
# архитектура x86_64; для ARM возьми соответствующий релиз
SSVER="v1.23.5"   # проверь актуальный на github.com/shadowsocks/shadowsocks-rust/releases
cd /tmp
curl -L -o ss.tar.xz \
  "https://github.com/shadowsocks/shadowsocks-rust/releases/download/${SSVER}/shadowsocks-${SSVER}.x86_64-unknown-linux-gnu.tar.xz"
tar -xf ss.tar.xz
sudo install -m755 ssserver /usr/local/bin/ssserver
ssserver --version
```

---

## 2. Сгенерировать ключ (для шифра 2022-blake3-aes-256-gcm)

Шифр `2022-blake3-aes-256-gcm` требует **ключ ровно 32 байта в base64** (не «пароль»):

```bash
openssl rand -base64 32
# пример вывода: 7yK2nT5pQwV8xR1mZ4cB9aS0dF6gH3jL2eU8iO5tP1=
```

Сохрани этот ключ — он понадобится и серверу, и в `ss://`-ссылке. На каждом сервере
свой ключ.

---

## 3. Конфиг сервера

`/etc/shadowsocks-rust/config.json`:

```json
{
  "server": "0.0.0.0",
  "server_port": 21043,
  "method": "2022-blake3-aes-256-gcm",
  "password": "7yK2nT5pQwV8xR1mZ4cB9aS0dF6gH3jL2eU8iO5tP1=",
  "mode": "tcp_only",
  "timeout": 300,
  "fast_open": false
}
```

Заметки:
- `password` здесь — это тот самый base64-ключ из шага 2.
- `mode: tcp_only` — IMAP/SMTP это TCP; UDP не нужен.
- `server_port` совпадает с открытым в фаерволе портом.

---

## 4. systemd-сервис

`/etc/systemd/system/ss-server.service`:

```ini
[Unit]
Description=Shadowsocks-rust server
After=network-online.target
Wants=network-online.target

[Service]
ExecStart=/usr/local/bin/ssserver -c /etc/shadowsocks-rust/config.json
Restart=on-failure
RestartSec=5
LimitNOFILE=51200
DynamicUser=yes

[Install]
WantedBy=multi-user.target
```

Запуск:

```bash
sudo systemctl daemon-reload
sudo systemctl enable --now ss-server
sudo systemctl status ss-server --no-pager
```

Фаервол (пример для ufw):

```bash
sudo ufw allow 21043/tcp
```

---

## 5. Собрать SIP002 `ss://`-ссылку

> **Важно для нашего кода:** формат строго **SIP002** — `ss://base64url(method:password)@host:port`.
> Наш `AutoProxyManager.proxyHostReachable()` парсит `URI.getHost()/getPort()`; со старым
> legacy-форматом `ss://base64(всё-целиком)` host не распарсится и прокси **никогда не
> включится**. Поэтому именно `userinfo@host:port`.

`userinfo` = base64**url**-без-паддинга от строки `method:password`:

```bash
METHOD="2022-blake3-aes-256-gcm"
PASSWORD="7yK2nT5pQwV8xR1mZ4cB9aS0dF6gH3jL2eU8iO5tP1="
HOST="45.144.169.117"     # IP или домен сервера
PORT="21043"

USERINFO=$(printf '%s:%s' "$METHOD" "$PASSWORD" | base64 -w0 | tr '+/' '-_' | tr -d '=')
echo "ss://${USERINFO}@${HOST}:${PORT}"
```

Пример результата:

```
ss://MjAyMi1ibGFrZTMtYWVzLTI1Ni1nY206N3lLMm5UNXBRd1Y4eFIxbVo0Y0I5YVMwZEY2Z0gzakwyZVU4aU81dFAxPQ@45.144.169.117:21043
```

Проверь, что строка парсится как валидный URI (host и port на месте) — это то, что
делает наш менеджер перед включением прокси.

---

## 6. Положить в `proxy.properties`

`proxy.properties` лежит в корне проекта (gitignored), формат — URL-ы через `;` или
с новой строки (`AUTO_PROXY_URLS`). Складываем туда все `ss://` со всех серверов:

```properties
AUTO_PROXY_URLS=ss://...@45.144.169.117:21043;ss://...@HOST2:21043;ss://...@HOST3:21043
```

На сборке они XOR+base64-обфусцируются в `BuildConfig.AUTO_PROXY_OBF`
(см. `build.gradle` → `autoProxyObfBlob()`), core декодирует и использует.

---

## 7. Проверка перед раздачей APK

Локально с реального устройства/сети (в идеале — из РФ-сети без VPN):

```bash
# собрать debug APK
./gradlew assembleFossDebug
```

Установить, выключить VPN, проверить что сообщения ходят. В логах (Settings →
Advanced → View log) при работающем Shadowsocks **не должно** быть
`Unexpected end of CONNECT response` — это была сигнатура мёртвого HTTP-прокси.

Быстрая проверка туннеля прямо с сервера/локалки (без приложения):

```bash
# поднять локальный socks5 поверх ss и постучаться в чатмейл-сервер
sslocal -b 127.0.0.1:1080 \
  -s 45.144.169.117:21043 \
  -m 2022-blake3-aes-256-gcm \
  -k "7yK2nT5pQwV8xR1mZ4cB9aS0dF6gH3jL2eU8iO5tP1=" &
curl -x socks5h://127.0.0.1:1080 -v https://alt-to.site:443 2>&1 | head
```

---

## 8. Эксплуатация

- **Ротация/выбытие IP:** когда IP сервера попадёт под блок — авто-прокси сам
  переключится на следующий живой `ss://` из пула. Держи 3–5 серверов, заменяй
  выбывшие новыми IP и пересобирай APK.
- **Мониторинг:** простой `systemctl status ss-server` + внешний пинг порта.
- **Безопасность ключей:** `proxy.properties` и серверные ключи в git **не** попадают.
  Ключ виден в APK только в XOR-обфусцированном виде (это не секрет уровня
  криптографии — лишь чтобы строки не лежали открытым текстом).

---

## 9. Ёмкость и масштабирование

Короткий ответ: **на одном дешёвом VPS спокойно живут ~1000–3000 одновременных
активных чатмейл-юзеров**, и упираешься ты не в его мощность, а в блокировку IP.

### Что нагружает сервер (по важности для нашего трафика)

**1. Трафик (полоса) — НЕ бутылочное горлышко.**
Чатмейл — почти-почта: текст это килобайты, между сообщениями соединение почти всё
время висит в IMAP IDLE и молчит. Реальный средний расход — единицы МБ на юзера в
день; даже щедро 50–100 МБ/день ≈ 1.5–3 ГБ/мес. Дешёвый Hetzner CX22 (€3.79: 2 vCPU /
4 ГБ / **20 ТБ трафика**) по полосе тянет **6000+ юзеров** — полоса кончится в
последнюю очередь.

**2. Одновременные соединения — реальный лимит.**
Каждый активный юзер держит через прокси 1–2 долгоживущих TCP-сессии (IMAP IDLE +
иногда SMTP). `shadowsocks-rust` асинхронный (tokio), простаивающее соединение стоит
копейки памяти. 4 ГБ RAM + поднятый лимит дескрипторов (`LimitNOFILE=51200` в юните) →
десятки тысяч сокетов технически, **тысячи юзеров** на практике.

**3. CPU на шифрование — спайки только в момент передачи.**
`2022-blake3-aes-256-gcm` с AES-NI (есть на любом современном x86 VPS) почти
бесплатен; грузит CPU только при реальной передаче байт, а у чатмейла это редкие
короткие всплески. 2 vCPU хватает на тысячи юзеров.

### Сводно (Hetzner CX22, €3.79/мес)

| Ресурс | Потолок по этому ресурсу |
|---|---|
| Трафик 20 ТБ/мес | ~6000+ юзеров |
| Одновременные соединения | тысячи (лимит — RAM/дескрипторы) |
| CPU (AES-NI) | тысячи |
| **Практически с запасом** | **~1000–3000 активных одновременно** |

### Важнее ёмкости — две вещи

**А. Не все юзеры идут через прокси.** Авто-прокси включается только у тех, у кого DPI
режет прямое соединение (РФ-сети в момент блокировки). Остальные ходят напрямую — на
серверы ляжет лишь часть аудитории.

**Б. Реальный ограничитель — блокировка IP, а не мощность.** РФ блокирует прокси **по
IP**, а не по нехватке ресурсов. Один IP рано или поздно попадёт под блок — и неважно,
что у него ещё есть запас по CPU. Поэтому масштабируемся **горизонтально: много
дешёвых VPS на разных IP/подсетях** (вариант B из `PROXY_DPI_ISSUE.md`). Это заодно
распределяет нагрузку: пул из 3–5 серверов авто-прокси раскидывает юзеров между живыми IP.

### Вывод по планированию

Считать «юзеров на сервер» по мощности почти бессмысленно — один CX22 за €4 переварит
больше, чем у вас сейчас всего юзеров. Планируй из расчёта **~500–1000 на сервер для
комфортного запаса** и держи **3–5 серверов на разных хостингах/IP** — не ради
нагрузки, а чтобы при блокировке одного IP остальные подхватывали. Бюджет на старте —
порядка **$15–25/мес за весь пул**.

Нюанс по хостингам: дешёвые провайдеры не любят прокси-трафик и реагируют на
abuse-жалобы. Hetzner относительно терпим, но IP под чатмейл держи «чистыми» и имей
под рукой замену выбывшим.

---

## Если plain Shadowsocks-2022 тоже начнут детектить

РФ-DPI развивается. Если со временем и SS-2022 начнёт резаться — следующий шаг в
рамках возможностей нашего core ограничен (HTTP/SOCKS5/SS). Тогда варианты:
- поставить перед SS обёртку типа `shadow-tls` / `sing-box` так, чтобы наружу
  смотрел обычный TLS на 443 (но core должен ходить в него как в SS/SOCKS5 локально —
  потребует доработки схемы);
- маскировать сам чатмейл-сервер за CDN/443 (см. Вариант C в `PROXY_DPI_ISSUE.md`).

Это уже отдельная история — на текущем этапе Shadowsocks-2022 на своих VPS закрывает
проблему.
